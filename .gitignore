# Paper source — keep the markdown, not the binary PDF
*.pdf

# Local virtualenv used for PDF conversion
.venv/

# Node deps installed on demand by gen_formulas.js
node_modules/

# macOS metadata
.DS_Store